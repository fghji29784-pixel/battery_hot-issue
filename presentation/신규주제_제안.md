# 15분 SDM 데이터로 무엇을 더 할 수 있는가 — 신규 주제 6건

> rev.1 / 기존 분석(내용정리 rev.5)이 끝난 지점에서 이어짐
> 목적: DOCV 와 자가방전 전류의 상관을 올리거나, 측정시간을 더 줄이거나,
>       현행 판정 방식의 구조적 한계를 메우는 **새 결과**를 만든다.

---

## ★ 실측 결과 (2026-09-15, 본체 41트레이 5,815셀) — 먼저 읽을 것

`correct.py` 를 실데이터로 돌린 결과다. 전문은 내용정리 **부록 9**.

| | 결과 |
|---|---|
| 트레이 오프셋의 온도 설명력 | **R² = 0.659** — 사전 기준 0.5 통과. **가설 성립** |
| ρ(dT/dt, I) | +0.485 |
| 음수 전류 | **26.9% → 2.7%** — 가설의 가장 깨끗한 증거 |
| delta_v 항등식 의심 | **기각**. 함의 저항 969 Ω ≠ 출력저항 0.5 Ω |
| 물리 보정이 TN 을 대체하는가 | **아니다.** 검출 ②TN 88/88 vs ④P2 62/75 |
| 형상 피처 전이 (제안 6) | **안 된다.** 본체에서 검출 0% |
| 셀 단위 보정 P1 | **쓰면 안 된다.** 원시보다 나쁘다 (12/38) |
| 트레이내 ρ | **0.048** — 검출은 되는데 상관은 사실상 0 |

**발표 문장은 "대체한다"가 아니라 "왜 듣는지 밝혔다 + 음수 전류를 없앴다"로 간다.**

### rank_tray.py 실측 (39트레이 5,533셀) — ★★ 판이 바뀌는 결과

| | 결과 |
|---|---|
| 쌍비교 순위모델 트레이내 ρ | **0.720** (원시 전류 0.048에서 급등) |
| 같은 모델, `--no-cond` | **−0.009** — 통째로 무너진다 |
| 가중치 상위 | `layer`, `dummy_L3`, `dummy_L2`, `dummy_L4` … 전류는 7위 |
| ρ 0.720 모델의 E 검출 | **38% / 50%** — ρ 0.048인 트레이 정규화 전류(88%/88%)보다 나쁘다 |

**트레이 안에서 ΔOCV 순위를 결정하는 것은 SDM 전류가 아니라 층(layer)이다.**
그리고 **층을 맞히는 것은 불량을 찾지 못한다.** ρ가 15배 높은 모델이 검출은 절반이다.

이것이 트레이내 ρ = 0.048의 정체다. 분해능 탓이 아니라, **ΔOCV 변동의 대부분이
불량과 무관한 층 효과와 잡음**이기 때문이다.

→ **목표를 "상관 올리기"로 두면 안 된다.** 상관은 층으로 얼마든지 올릴 수 있고,
그렇게 올린 상관은 선별을 오히려 악화시킨다. 목표는 선별 성능이어야 한다.

→ **새 주제가 하나 생겼다: 판정 단위를 "트레이"가 아니라 "트레이 × 층"으로
바꿔야 하는가.** `rank_tray.py [3]` 이 η²와 층별 ΔOCV 프로파일, 그리고
트레이 정규화 vs 트레이×층 정규화의 검출 비교를 직접 낸다.

### ★★★ 절제 실험이 판을 바꿨다 — 목표를 다시 써야 한다

| 쓴 피처 | 트레이내 ρ | 상위1% E | 상위5% E |
|---|---|---|---|
| 전체 피처 | 0.723 | 38% | 50% |
| 층 계열 제외 (= 전류 + 온도·전압) | 0.420 | **0%** | **0%** |
| 조건 전체 제외 (= 전류만) | **0.005** | 50% | 62% |
| (참고) 트레이 정규화 전류 | 0.048 | **88%** | **88%** |

**SDM 전류만으로 남는 상관은 0.005다.** 전체 0.723의 1%. 나머지 99%는 측정이 아니라
조건 변수가 만든 것이다. 발표에서 쓸 숫자는 0.005 쪽이다.

**그리고 더 중요한 것 — 조건 피처는 검출을 파괴한다.** 온도·전압을 넣으면 검출이
**0%**가 된다. 그 피처들이 ΔOCV 순위는 잘 맞히지만(ρ 0.420) 그 순위가 불량과 반대
방향이라, 모델이 불량 셀을 아래로 밀어낸다.

> ### 발표 메시지
> **"ΔOCV를 예측하려 하지 마십시오. 예측이 좋아질수록 불량 검출은 나빠집니다."**
>
> ρ가 0.005 → 0.723으로 **145배** 오르는 동안 검출은 50%/62% → 38%/50%로 **떨어진다.**
> 상관이 가장 낮은 트레이 정규화 전류(ρ 0.048)가 검출은 88%/88%로 **최고**다.

이 결론은 문헌과도 맞는다. Liu 2022(J. Energy Storage 55, 105571)의
*"BP 신경망과 GPR은 전반적 추세는 예측했으나 고값을 예측할 방법이 없었고 이상 셀을
식별하지 못했다"* 를 **자사 데이터에서 정량적으로 재현**한 것이다.
각주 인용이던 [4]번 논문이 본문으로 올라갈 근거가 생겼다.

**목표를 다시 쓴다**

- ~~15분 SDM으로 3일 ΔOCV를 예측한다~~
- ~~DOCV와 자가방전 전류의 상관을 높인다~~
- **15분 SDM으로 저전압 불량을 선별한다 — 상관은 지표가 아니다**

### ☠→✅ 발표 중심 결론 검증 완료 — 재작성이 필요하다

`classify.py` 기본 vs `--no-cond` (41트레이 5,815셀, 불량 8개)

| | 기본 (조건 13개) | `--no-cond` (전류만) |
|---|---|---|
| PR-AUC | 0.1835 | **0.2868** (56% 높다) |
| ROC-AUC | 0.9732 | 0.9491 |
| 상위 5% (291셀) | 7/8 | 7/8 |
| 상위 20% (1,163셀) | **8/8** | 7/8 |
| 100% 검출 필요 검사 비율 | **19.85%** | 36.87% |

**"20% 검사로 100% 검출"은 조건 피처가 만든 것이다.** 전류만 쓰면 20%에서 7/8이고
8/8에는 36.87%가 필요하다. 다만 **그 차이는 불량 8개 중 1개**다. 한계 ①에 이미 적었듯
7/8과 8/8은 셀 하나 차이로 통계적으로 구분되지 않는다 — 조건 피처가 8번째를 잡아준다는
주장 자체가 표본 1개에 걸려 있다.

**그런데 PR-AUC는 전류만 쓸 때가 훨씬 높다.** 희소 불량에서는 ROC-AUC보다 PR-AUC가
믿을 만하다. ROC-AUC는 음성 5,807개를 잘 정렬해도 올라가지만, 우리가 보는 것은 양성
8개의 위치다. 조건 피처는 **가장 애매한 1개만 끌어올리고 나머지는 흐트러뜨린다.**

### ★★★ 그리고 5분이면 충분하다 — 이번 세션 최대 성과

구간별 PR-AUC

| 구간 | 기본 | `--no-cond` |
|---|---|---|
| **5분** | 0.1497 | **0.4396** ← 최고 |
| 8분 | 0.1417 | 0.3706 |
| 10분 | 0.2144 | 0.3785 |
| 15분 | 0.1486 | 0.2322 |
| 30분 | 0.2868 | 0.2868 |

전류만 쓰면 **5분이 전 구간 최고**이고, 기본 모델의 어떤 구간(최고 0.2144)보다 **2배 이상**
높다. 그리고 **시간이 길어질수록 나빠진다** (0.4396 → 0.2868).

**왜 길수록 나빠지는가 — `correct.py` 결과와 정합한다.** 트레이간 분산 비중이
5분 70.3% → 15분 75.6%로 늘어난다. 열드리프트는 시간에 따라 누적되므로 **초기 전류가
덜 오염된 신호**다. 정착을 기다릴수록 인공전류가 쌓인다. 부록 9의 열드리프트 가설이
여기서 독립적으로 재확인된다.

### 운영 결론 재작성

| | |
|---|---|
| ~~폐기~~ | ~~15분 측정 + 전체 피처 모델 + 20% 검사 → 불량 전량 검출~~ |
| **신규** | **5분 측정 + SDM 전류만 + 상위 5% 검사 → 7/8 검출** |

- 측정시간 **15분 → 5분** (1/3)
- 검사 비율 **20% → 5%** (1/4)
- PR-AUC **0.1835 → 0.4396** (2.4배)
- 조건 변수에 기대지 않으므로 라인·트레이가 바뀌어도 성립할 여지가 크다

**8번째 불량은 어느 방식으로도 안정적으로 잡히지 않는다.** 잡았다고 말하는 대신
잡지 못한다고 말하고, 불량 표본 확대(한계 ④)를 다음 단계로 제시하는 편이 정직하고 안전하다.

### (검증 완료) 원래 적어둔 확인 항목

부록 6의 **③ 전체 피처 모델**(ROC-AUC 0.9732, **20% 검사로 8/8 검출**)은 조건 피처를
포함한다. 그 성능이 전류에서 온 것인지 조건 변수에서 온 것인지 **아직 가르지 않았다.**
★ 운영 결론("20%만 검사하면 불량을 전부 잡는다, 나머지 80%는 3일 보관 생략")이
여기에 걸려 있다.

```bash
python analysis/classify.py "데이터.xlsx"
python analysis/classify.py "데이터.xlsx" --no-cond
```

비슷하면 결론은 살아남는다. `--no-cond`가 크게 나빠지면 그 모델은 자가방전이 아니라
측정 조건을 학습한 것이므로 결론을 다시 써야 한다. **이것이 지금 가장 급한 확인이다.**

### ★★★ 안 잡히는 셀 1개의 정체 — 열드리프트가 부호를 뒤집었다

`defects.py` 실측. **셀 140 (CFDD011456) 하나가 모든 지표를 망칩니다.**

| 셀 | 원시 전류 | 트레이 정규화 | 물리보정 P2 | 전류만 모델 |
|---|---|---|---|---|
| 나머지 7개 최악 | 24.33% | 0.50% | 34.08% | **3.54%** |
| **140** | 85.16% | **97.49%** | 87.62% | 36.49% |

**셀 140을 빼면 7개가 전류만 모델 상위 3.54%에 전부 들어옵니다.**
부록 12의 "상위 5%에서 7/8"보다 정밀한 숫자입니다.

그 셀의 정체가 [3]에 그대로 나옵니다.

| 항목 | 이 셀 | 다른 불량 중앙 | 양품 중앙 |
|---|---|---|---|
| 전류 15분 | **−1.666e−06** | 1.180e−05 | 2.878e−06 |
| 트레이대비 전류 | **−4.095e−06** | 6.157e−06 | 0 |
| t_init | **25.63** | 24.88 | 25.1 |
| delta_t | **−0.09735** | +0.02111 | −0.05187 |
| ΔOCV | 2.136 | 2.413 | 1.366 |

**전류가 음수입니다.** 양품 쪽에 가까운 정도가 아니라 **양품보다 낮습니다.**
SDM이 이 셀을 "자가방전이 가장 적은 셀"로 읽었는데, 3일 후 ΔOCV는 양품보다 높았습니다.

**원인이 조건 변수에 그대로 찍혀 있습니다.** `t_init` 최고(뜨겁게 들어옴),
`delta_t` 최저(가장 많이 식음). 부록 9의 ρ(dT/dt, I) = +0.485를 대입하면
**가장 많이 식은 셀은 전류가 가장 낮게 읽힙니다.** 열드리프트 인공전류가
자가방전 신호를 덮고도 남아 **부호를 뒤집은** 겁니다.

→ **P2로는 못 고칩니다** (87.62%). P2는 트레이 중앙 조건만 쓰는데,
이 셀은 **트레이 안에서 혼자** 열평형이 안 된 셀입니다.
**셀 단위 보정 P1이 맞는 도구일 수 있습니다.** `defects.py`에 P1 열과
"자기 트레이 안에서 몇 등인가" 표를 추가했으니 다시 돌려보시면 답이 나옵니다.

### ★ 5분 결론은 최악 트레이에서도 살아남는다 (근거는 바꿔야 한다)

| 구간 | 중앙 ρ | 하위10% 트레이 |
|---|---|---|
| 5분 | 0.818 | **0.632** |
| 15분 | 0.949 | 0.913 |
| 30분 | 1.000 | 1.000 |

전체 순위 기준으로는 5분이 부족합니다. **그런데 검출은 다릅니다.**

| 방식 | 평균 측정시간 | 상위1% E | 상위5% E |
|---|---|---|---|
| 고정 5분 | 5.0분 | 62% | **88%** |
| 고정 30분 | 30.0분 | 62% | **88%** |
| 적응형 (0.98) | 20.3분 | 62% | 88% |

**5분과 30분의 검출이 완전히 같습니다.** 전체 순위가 굳는 데는 30분이 걸리지만
**상위 극소수의 자리는 5분에 이미 잡힙니다.** 우리가 쓰는 건 후자입니다.

→ 5분 결론은 유지. 단 근거를 "순위 안정화"가 아니라 **"검출 포화"**로 말해야 합니다.

### ★ 측정 건전성 — 재측정 권고 13/41 트레이 (32%)

음수 비율 중앙 0.076인데 **90분위가 0.894, 최악 1.000**입니다.
**일부 트레이는 거의 모든 셀이 음수 전류**입니다 — 셀 140과 같은 기구가
트레이 전체에 걸린 경우입니다. 그 트레이 결과는 그대로 쓰면 안 됩니다.

이것만으로 발표 한 장이 됩니다: **"지금은 모든 트레이 결과를 같은 신뢰도로 쓰고 있습니다."**

### ★★★ 히트맵이 연 새 길 — 자리(位置)로 보정한다

트레이 히트맵에서 두 가지가 보였습니다.

| | A행 쪽 | L행 쪽 |
|---|---|---|
| 전류 | +4.5 ~ +3.9 µA | **−0.1 ~ −6.5 µA** |
| 냉각량 (T_start−T_final) | 0.03 ~ 0.05 ℃ | **0.11 ~ 0.16 ℃** |

**두 히트맵이 정확히 반대 패턴입니다.** 많이 식는 자리일수록 전류가 낮게 읽힙니다.
부록 9의 ρ(dT/dt, I) = +0.485를 **트레이 하나 안에서 그림으로** 본 것입니다.

→ 열드리프트 가설이 이제 **세 층위**에서 확인됐습니다:
**트레이 간**(부록 9) · **셀 단위**(부록 15) · **트레이 내 공간**(부록 16).
앞의 둘은 상관계수였고 이건 그림입니다. 발표에서 훨씬 셉니다.

**그리고 셀 번호가 곧 자리입니다.** `140 → L08` (열-우선 12×12).
불량 8개 자리: `E01 L06 L04 L08 H04 F05 E09 B07` — **L행에 3개**,
히트맵에서 전류가 음수인 바로 그 구역입니다.

> **온도를 재서 보정하는 대신 "자리"로 보정할 수 있을지 모릅니다.**
> 온도 측정은 셀당 ±0.05 K로 요동하지만 자리는 정확하고,
> **이미 데이터에 들어 있어 추가 계측 비용이 0**입니다.

**☠ 단, 전제가 있습니다 — 그 구배가 트레이마다 같아야 합니다.**
트레이 하나의 히트맵만 보고 단정할 수 없습니다. 챔버 위치·투입 방향·대기시간이
다르면 구배도 다르고, **부호가 뒤집히면 전체 공통 보정은 오히려 해가 됩니다.**

합성 데이터로 실증했습니다 (최악셀 검사율, 낮을수록 좋음)

| 구배 방향 | ① 보정 없음 | ② 전체 공통 | ②' 트레이별 |
|---|---|---|---|
| 모두 같을 때 | 37.08% | 50.28% | **30.10%** |
| 절반 뒤집힐 때 | 86.32% | **96.18%** ☠ | **28.72%** |

구배가 뒤집히면 **전체 공통 보정이 아무 보정 안 한 것보다 나쁩니다**(86 → 96%).
**트레이별 보정은 두 경우 모두 안전합니다.**

→ **기본은 트레이별 보정.** 전체 공통은 `[2-b]` 전제 검정을 통과했을 때만.
→ 단 트레이별 보정에도 대가가 있습니다. **그 트레이 안의 진짜 공간 불량까지
같이 지워집니다.** 불량 3개가 L행에 몰려 있었으니 반드시 함께 봐야 합니다.

### ★ 셀 140의 곡선은 U자다 — 못 본 게 아니라 가려진 것이다

전류 `0 → −1.8 µA(15분) → −0.2 µA(30분)`. 다른 셀은 전부 단조 증가인데
이 셀만 내려갔다 올라옵니다. 온도가 떨어지는 동안 음의 인공전류가 실리고,
온도가 안정되면 그 항이 사라지면서 진짜 자가방전이 드러납니다.

→ **"SDM이 못 본 셀"이 아니라 "SDM은 봤는데 온도가 가린 셀"입니다.**
값(`i_15min`)으로 읽으면 양품보다 낮지만, **형상**으로 읽으면 다릅니다.

### `rescue.py` — 두 가지를 시험합니다

```bash
python analysis/rescue.py "데이터.xlsx"
```

여섯 점수의 **"최악셀 검사율"**(불량을 전부 잡는 데 필요한 검사 비율)을 비교합니다:
① 트레이 정규화 · ② **+ 자리 보정** · ③ 후반 기울기 · ④ ③+자리보정 ·
⑤ **최저점 대비 회복량** · ⑥ ②+⑤

합성 데이터(공간 구배를 심은 것)에서 행 설명 비중 95.9%, 냉각량 상관 −1.000을
정확히 잡아냈고 최악셀 검사율이 **81.98% → 58.72% → 16.74%**로 줄었습니다.
**이 수치는 합성값입니다** — 실데이터는 직접 돌려봐야 합니다.

### 이것이 만드는 공정 액션

트레이 안에 온도 구배가 있다는 건 **챔버 공기 흐름이나 적재 방식**의 문제입니다.
그걸 줄이면 **보정 없이도** 측정 품질이 올라갑니다. 분석 개선이 아니라 설비 개선입니다.

### 기각·축소된 제안

| 제안 | 실측 결과 | 조치 |
|---|---|---|
| **제안 3** median/MAD | 적출 36→68셀(+89%), E 검출 8/8 → 8/8 **변화 없음** | **기각.** 과검만 늘었다. 자기가림만 없애는 **LOO(36→40)** 로 축소 |
| **제안 4** 계층 판정 | max로 합치면 상위1% 검출 50% → **12%** | **구현 오류.** 트레이 간 z는 트레이 내 모든 셀이 같은 값이라 셀을 구별 못 한다. 두 z를 **분리 사용**하도록 수정 |
| **제안 6** 형상 피처 | 본체 검출 0% | **기각.** 보조→본체 전이 안 됨 |
| **트레이 × 층 판정** (직전 턴의 새 제안) | η² 7.7%로 층 효과는 실재하나 검출 88/88 → 88/88 **변화 없음** | **기각.** 판정 단위는 트레이 그대로. 층은 모델 피처에서 **빼야** 한다 |
| **제안 5** 적응형 측정시간 | 20.3분 써서 고정 5분과 같은 검출(62/88) | **기각.** 시간 단축의 답은 적응형이 아니라 **"그냥 5분 고정"**. 구현도 설명도 쉽다 |
| **제안 5-2** 2단 선별 | 고정 5분이 이미 88% | **기각.** 같은 이유 |


아래 제안 1의 "트레이 중앙값 빼기를 대체할 수 있다"는 이 데이터에서 성립하지 않았다.
설명은 되지만 대체는 안 된다. 제안 4(계층 판정)는 그래도 유효하다 —
TN 이 원리적으로 못 보는 트레이 간 신호는 물리 보정 뒤에만 볼 수 있기 때문이다.

한편 **⑤ P2 + 아레니우스가 트레이내 ρ 를 0.048 → 0.067 로 올렸다.**
목적 지표를 개선한 유일한 항목이고, 아레니우스 보정은 열드리프트를 먼저 뺀 뒤에만
듣는다는 뜻이다. 다음 확인 대상이다.

---

## 0. 지금까지 남은 것과 남지 않은 것

기존 분석이 확보한 것은 하나다. **트레이 정규화 하나로 상위 0.5% 검출이 25% → 87.5%.**
그런데 이건 "그렇게 하면 잘 되더라"이지, **왜 되는지**가 아니다. 그리고 미해결로
명시해 둔 항목이 셋 남아 있다.

| 내용정리 항목 | 미해결로 남은 것 |
|---|---|
| 한계 ② | 전압강하량 회귀 실패. 결정계수가 음수 |
| 한계 ⑤ | 전류-온도 상관 −0.674. **아레니우스와 반대 부호** |
| 부록 4 | 음수 전류 비율 24~31%. 트레이간 분산 비중이 1.2%~68.7% 로 **시점마다 요동** |

이 셋은 따로 있는 문제가 아니다. **하나의 원인으로 전부 설명된다**는 것이
아래 제안 1의 내용이고, 이번 제안 묶음의 중심이다.

> ⚠️ 이 문서의 스크립트는 합성 데이터로 동작만 확인했다.
> `analysis/synth.py` 가 만드는 수치는 **전부 가짜**이므로 발표에 쓰지 말 것.
> 실제 엑셀로 돌린 결과만 쓴다.

---

## 제안 1 ★ 음수 전류의 정체는 열드리프트다 — 물리 보정

**실행** `python analysis/correct.py "데이터.xlsx" --sweep`

### 가설

전위고정 SDM 에서 계측기가 읽는 전류는 자가방전만이 아니다.

```
I_meas(t) = I_sd(t)  +  C_eff × (dU/dT) × (dT/dt)  +  잡음
            ~~~~~~~     ~~~~~~~~~~~~~~~~~~~~~~~~~
            원하는 것    셀 온도가 변하면 OCV 가 따라 움직이고,
                        전압을 붙들기 위해 계측기가 전류를 흘린다
```

32Ah 셀의 C_eff 는 10⁵ F 급이다. dU/dT 가 0.1 mV/K 수준이어도
**0.01 K/min 의 드리프트가 수 µA 의 가짜 전류를 만든다.**
자가방전 신호 자체가 수 µA(본체 30분 중앙값 5.96 µA)이므로,
이 항은 무시할 수 있는 크기가 아니라 **신호와 같은 크기**다.

### 이 하나로 셋이 동시에 설명된다

1. **음수 전류** — dT/dt 의 부호를 따라간다. 셀이 식으면 음수가 나온다.
2. **트레이별 전류대 차이** — 트레이마다 투입 시각·대기시간·챔버 위치가 다르다.
   온도 이력은 셀이 아니라 **트레이 단위로 결정**된다.
3. **온도 상관 −0.674** — 온도는 두 경로로 전류에 들어온다.
   아레니우스는 **정적**이고 부호가 +, 열드리프트는 **동적**이고 부호가 자유다.
   지금 데이터는 후자가 지배적이라 합쳐진 부호가 뒤집힌 것이다.
   (t_init −0.674 와 delta_t +0.498 이 서로 모순이 아니라 **같은 말**이라는 점이 방증:
    뜨겁게 들어온 셀일수록 더 식으므로 delta_t 가 음수다.)

### 방법 — 계수는 '트레이 중앙값끼리' 회귀해서 얻는다

셀 단위로 회귀하면 셀 고유의 자가방전 신호까지 같이 지워질 위험이 있다.
그래서 **트레이 중앙값 대 트레이 중앙값**으로 회귀해 계수 β 를 얻는다.
셀 고유 편차는 회귀에 참여하지 않으므로 지워지지 않는다.

| | 보정식 | 트레이 내 순위 |
|---|---|---|
| **P1** 셀 단위 | `I − (x_cell − x_ref) · β` | 바뀐다 |
| **P2** 트레이 오프셋만 | `I − (x_tray − x_ref) · β` | **그대로** |

기준점 x_ref 는 dT/dt = 0, T = 25 ℃ 로 둔다. 결과는 "열평형 25 ℃ 조건의 전류"로 읽힌다.

**P2 가 이 제안의 핵심이다.** 트레이 중앙값 빼기(TN)의 드롭인 대체품인데,
TN 과 달리 **온도로 설명되는 만큼만** 뺀다.

### 성공 / 반증 조건

| | 성공 | 반증 |
|---|---|---|
| ρ(dT/dt, I) | \|ρ\| > 0.3 | 0 근처 |
| 트레이 중앙값 회귀 R² | > 0.5 | < 0.3 |
| 보정 후 음수 비율 | 뚜렷이 감소 | 그대로 |
| P2 의 DOCV 상관 | ① 원시보다 개선 | 개선 없음 |

**반증되어도 발표거리다.** "음수 전류가 온도 탓이 아니라면 무엇인가"로
문제가 좁혀지고, 그 자체가 다음 과제의 정의가 된다.

### 발표에서 쓸 한 문장

> **"트레이 정규화가 듣는 이유는 트레이가 아니라 온도였습니다."**

### 왜 이게 값어치가 있는가

분석 개선에서 끝나지 않고 **공정 액션**이 나온다.
트레이 오프셋이 온도 이력으로 설명된다면, SDM 투입 전 **대기시간 표준화**만으로
측정 품질이 올라간다. 설비 구매도 알고리즘 도입도 필요 없다.

---

## 제안 2 ★ 회귀는 실패한 게 아니라 잘못 세워진 문제였다

**실행** `python analysis/rank_tray.py "데이터.xlsx"`

### 문제 재정식화

현행 판정은 **값**이 아니라 **그 트레이 안에서 튀는가**다.
그러면 맞혀야 할 것도 값이 아니라 **트레이 내 순위**다.

```
회귀    : 모든 셀의 mV 를 맞힌다 → 트레이 기준선(전압대)이 오차의 대부분을 먹는다 → R² < 0
순위학습 : 같은 트레이 셀끼리 누가 더 나쁜지만 맞힌다 → 기준선이 차분으로 사라진다
```

같은 트레이 안에서 쌍 (a, b) 를 뽑아 `sign(DOCV_a − DOCV_b)` 를 맞히는
**선형 쌍비교 모델**(RankNet 의 선형판)을 학습한다. 점수는 `w · x` 로 분해되므로
가중치가 그대로 해석된다 — 발표에서 설명할 수 있는 모델이다.

### 왜 새로운가

기존 분석은 "회귀가 실패했으니 지표를 선별로 바꿨다"였다. 후퇴다.
이건 **"목적에 맞게 다시 세우면 같은 데이터에서 성립한다"** 는 전진이고,
한계 ②를 정면으로 닫는다.

### 보고할 지표 — 새로 정의한 것

- **트레이내 ρ** : 트레이 안에서만 잰 순위상관을 Fisher-z 로 합침.
  전체 풀링 상관은 트레이 간 오프셋에 오염되므로 목적과 어긋난다.
  DOCV-전류 상관을 **이 지표로 다시 재는 것만으로도 숫자가 달라진다.**
- **트레이내 R²** : 트레이 기준선을 뺀 뒤의 결정계수. 음수였던 값이 양수로 나오면
  "회귀 실패"가 아니라 "기준선을 못 맞힌 것"이었음이 증명된다.

`--no-cond` 로 조건 피처(온도·전압)를 빼고 한 번 더 돌릴 것.
성능이 무너지면 모델이 자가방전이 아니라 **온도를 학습한 것**이므로,
제안 1의 물리 보정을 먼저 넣어야 한다. 이 확인을 건너뛰면 안 된다.

---

## 제안 3 ★ 판정식 한 줄 — μ+3σ 의 자기가림

**실행** `python analysis/rank_tray.py "데이터.xlsx"` 의 `[1]`

### 무엇이 문제인가

μ+3σ 는 임계값을 계산할 때 **판정 대상 셀 자신을 통계량에 넣는다.**
크게 튀는 셀일수록 자기 트레이의 σ 를 키워 **스스로를 임계 밖으로 밀어낸다.**
트레이당 셀이 적을수록 심하다 (본체 기준 트레이당 약 142셀).

세 가지를 같은 데이터로 비교한다.

| | 규칙 | 자기가림 |
|---|---|---|
| 현행 | μ + 3σ (자기 포함) | 있음 |
| 개선 A | μ + 3σ (자기 제외, LOO) | 없음 |
| 개선 B | **median + 3 × 1.4826 MAD** | 원리적으로 없음 |

현행과 개선 B 의 적출 결과 차이가 곧 지금 놓치고 있는 셀이다.

### 왜 값어치가 있는가

**측정도 설비도 바꾸지 않는다. 판정식 한 줄이다.**
Zheng 2026 이 쓴 트레이별 μ+3σ 규칙을 그대로 따르되 통계량만 로버스트하게 바꾸는 것이라
문헌과도 어긋나지 않는다. 발표에서 "오늘 당장 적용 가능한 것"으로 내놓을 수 있는
유일한 항목이다.

---

## 제안 4 ★ 트레이 상대평가의 사각지대 — 계층 판정

**실행** `correct.py` 의 `[4]` + `rank_tray.py` 의 `[3]`

### 구조적 한계

트레이 상대평가는 **트레이 전체가 나쁜 경우를 원리적으로 못 잡는다.**
다 같이 나쁘면 아무도 튀지 않는다. TN 은 트레이 중앙값을 0 으로 만들어버리므로
트레이 간 비교 자체가 불가능해진다.

이건 성능 문제가 아니라 **정의상의 맹점**이다. 데이터를 아무리 늘려도 안 닫힌다.

### 해법

제안 1의 물리 보정을 먼저 한 뒤 **두 층으로 판정**한다.

```
트레이 내 z  = (I_corr − median_tray) / (1.4826 MAD_tray)     ← 셀 단위 불량
트레이 간 z  = (median_tray − median_all) / (1.4826 MAD_all)  ← 트레이 단위 불량
판정 = max(둘)
```

트레이 간 z 는 **온도 보정 없이는 의미가 없다**. 보정 전에는 그냥 온도 차이를
읽을 뿐이다. 그래서 제안 1이 선행 조건이다. 두 제안이 한 묶음인 이유다.

### 발표에서 쓸 한 문장

> **"지금 방식은 잘못된 트레이 하나를 통째로 통과시킵니다. 구조적으로 그렇습니다."**

---

## 제안 5 ★ 몇 분이 필요한지는 트레이마다 다르다 — 적응형 측정

**실행** `python analysis/adaptive.py "데이터.xlsx"`

### 발상

지금은 모든 트레이를 똑같이 15분(또는 30분) 측정한다.
온도가 이미 안정된 트레이는 5분이면 순위가 굳고, 막 투입돼 식고 있는 트레이는
20분이 지나도 흔들린다. 똑같이 주는 것은 **앞쪽엔 낭비, 뒤쪽엔 부족**이다.

판정이 트레이 상대평가이므로 멈출 시점의 정의가 분명해진다.

> **순위가 더 이상 바뀌지 않으면 멈춘다.**

이 규칙은 **정답(3일 ΔOCV)을 몰라도 측정 중에 계산할 수 있다.**
트레이 안에서 t분 순위와 t−2분 순위를 비교하면 되기 때문이다.
그래서 양산에 그대로 얹을 수 있다. 기존 "10분이면 포화"는 사후 분석이라
현장에서 쓸 수 없었다 — 이 차이가 이 제안의 값어치다.

정지 기준 두 가지를 모두 계산하고 θ 민감도표를 낸다.

- **전체순위 기준** : 트레이 내 Spearman(t, t−2) ≥ θ 가 연속 2회
- **적출후보 기준** : 트레이 상위 5% 셀 **집합**의 Jaccard ≥ θ 가 연속 2회
  → 중간 순위의 잡음에 둔감하다. 판정은 상위만 쓰기 때문에 이쪽이 목적에 맞다.

θ 를 미리 정하지 않는다. 표에서 **"최악 트레이의 최종순위 재현도"가 기준선 아래로
내려가지 않는 선에서 평균 측정시간이 가장 짧은 칸**을 고른다 (`--floor`).

### 덤 — `[1]` 이 기존 결론을 보강한다

기존 "10분이면 30분과 같다"는 **전체 평균**이다.
전수 검사의 실패는 평균이 아니라 **최악의 트레이**에서 난다.
`[1]` 은 하위 10% 트레이 기준으로 다시 계산하므로, 고정 시간을 정해야 한다면
그 숫자가 근거가 된다. 발표의 D 항목을 한 단계 단단하게 만든다.

### 덤 2 — 측정 건전성 지표 (`[4]`)

트레이 단위로 **믿어도 되는지**를 판정한다.

| 지표 | 조치 |
|---|---|
| \|dT/dt\| 상위 10% | 대기 후 재측정 — 열평형 전에 넣은 트레이 |
| 순위 안정도 < 0.95 | 측정 연장 — 아직 순위가 흔들린다 |
| 음수 비율 > 50% | 결과 폐기 — 열드리프트가 신호를 덮었다 |

지금은 **모든 트레이의 결과를 같은 신뢰도로 쓴다.** 트레이마다 신뢰도가 다르다는 것을
수치로 말할 수 있으면 그것만으로 오판이 준다. 이건 모델이 아니라 **운영 규칙**이라
도입 장벽이 가장 낮다.

---

## 제안 6 형상 피처를 본체 데이터에 아직 안 써봤다

**실행** `correct.py` 의 `[3]` 표 ⑦⑧행

보조 데이터 35셀에서 **비율 피처 I(끝)/I(50%) 가 단일 피처 최고**였다 (ρ 0.738 vs 값 0.673).
이유도 규명돼 있다 — 부록 2 [H1] 의 ρ(τ, I_inf) = +0.712,
즉 자가방전이 큰 셀일수록 정착이 느려 두 효과가 상쇄되므로,
**값보다 비율이 정착값을 더 잘 반영한다.**

그런데 **본체 5,815셀 분석에서는 비율 피처를 한 번도 쓰지 않았다.**
`predict_xlsx.py` 는 "비율·로그는 쓰지 않는다"고 명시했고, `classify.py` 도
i_Xmin + slope + 트레이 정규화본만 쓴다.

**가장 싼 미시도 실험이다.** 이미 있는 데이터, 이미 규명된 근거, 코드는 한 줄.
안 되면 "보조 데이터의 발견이 본체로 전이되지 않는다"는 것 자체가 보고할 결과다
(셀 종류·측정 조건 차이 때문일 수 있고, 그러면 보조 데이터 기반 결론 전체를
다시 봐야 한다 — 이것도 중요한 발표거리다).

---

## 발표 전 반드시 확인할 것 — delta_v 는 독립 피처가 아닐 수 있다

부록 4 에서 `ρ(delta_v, I) = +0.958` 을 **상관**으로 적었다.
그런데 출력저항 R_out 을 통한 **옴의 법칙으로 묶인 같은 측정**일 가능성이 크다.

`correct.py [1]` 이 `median(delta_v / I)` 를 함의 저항으로 출력한다.
이 값이 장비 출력저항(0.5 Ω)과 같으면 둘은 같은 측정이고, 그러면

- 피처 목록에서 delta_v 를 빼야 한다
- 피처 기여도 해석이 달라진다
- **"두 값이 0.958 로 상관"은 발견이 아니라 항등식**이므로 발표에서 말하면 안 된다

이건 새 주제가 아니라 **기존 자료의 오류 가능성**이다. 먼저 확인할 것.

---

## 우선순위와 실행 순서

| 순서 | 할 일 | 명령 | 걸리는 시간 |
|---|---|---|---|
| 1 | **제안 1 검정** — 여기서 발표 주제가 정해진다 | `correct.py "데이터.xlsx" --sweep` | 수 분 |
| 2 | delta_v 항등식 확인 | 위 `[1]` 출력 | 즉시 |
| 3 | **제안 3** — 당장 적용 가능한 개선 | `rank_tray.py "데이터.xlsx"` `[1]` | 수 분 |
| 4 | **제안 2** — 한계 ② 닫기 | `rank_tray.py "데이터.xlsx"` + `--no-cond` | 수 분 |
| 5 | **제안 5** — 시간 단축 | `adaptive.py "데이터.xlsx"` | 수 분 |
| 6 | 제안 4·6 — 위 출력에 이미 포함 | — | — |

**1번 하나로 발표의 중심이 갈린다.**

- R² > 0.5 → **제안 1을 중심에 놓는다.** "왜 트레이 정규화가 듣는가"에 답하는
  메커니즘 발표가 되고, 제안 4가 따라붙는다. 기존 rev.5 자료의 중심이 바뀐다.
- R² < 0.3 → 제안 1을 **반증된 가설**로 한 장만 쓰고, 중심을 **제안 2+3+5**
  (문제 재정식화 · 판정식 개선 · 적응형 측정)로 옮긴다. 이쪽도 충분히 성립한다.

---

## 새 제안에도 그대로 걸리는 제약

기존 한계 ①·③·④ 는 새 제안에도 똑같이 적용된다.

- **불량 8개**. 새 방법의 검출률도 신뢰구간이 넓다.
  → 그래서 이번 제안들은 **검출률 경쟁이 아니라 메커니즘 규명과 운영 규칙**에
    무게를 뒀다. 제안 1·3·4·5 는 불량 개수에 거의 의존하지 않는다.
    제안 1의 R², 제안 3의 적출 차이, 제안 5의 순위 안정도는
    **양품 5,807개만으로 계산되는 값**이다. 이게 이 묶음의 설계 의도다.
- **표본 크기는 트레이 수(41)** 가 결정한다. 셀 수가 아니다.
  제안 1의 트레이 중앙값 회귀는 n = 41 이므로 설명변수를 5개 이상 넣지 말 것.
- 검증 분할은 트레이 단위. 새 스크립트 전부 `GroupKFold(groups=tray)` 를 쓴다.

---

## 데이터 요청 — 하나만 고르면 이것

> **SDM 장비의 분단위 온도 로그 (`t_5min` … `t_30min`)**

지금 본체 엑셀에는 `t_init` / `t_final` 둘뿐이라 dT/dt 를 **30분 평균 기울기 하나로만**
근사한다. 열드리프트는 지수적으로 감쇠하므로 이건 거친 근사다.
분단위 온도가 있으면 시점마다 dT/dt 를 구해 **시점별로 보정**할 수 있고,
제안 1의 R² 가 올라갈 여지가 크다. `correct.py` 는 `t_XXmin` 컬럼이 있으면
**자동으로 그쪽을 쓴다.** 컬럼만 추가하면 된다.

그 다음 순위로 필요한 것: 트레이 투입 시각, 챔버 내 위치, SDM 트레이와 3일 보관
트레이의 동일 여부.

---

## 새로 추가된 스크립트

| 파일 | 하는 일 | 대응 제안 |
|---|---|---|
| `analysis/correct.py` | 열드리프트 물리 보정, 8개 점수 비교, 계층 판정 근거 | 1, 4, 6 |
| `analysis/rank_tray.py` | 자기가림 진단, 트레이 내 쌍비교 순위학습, 계층 판정 | 2, 3, 4 |
| `analysis/adaptive.py` | 적응형 정지 규칙, θ 민감도, 2단 선별, 측정 건전성 | 5 |
| `analysis/synth.py` | 동작 확인용 합성 데이터 (**수치는 전부 가짜**) | — |
| `analysis/runlog.py` | 실행 결과 자동 저장 (`analysis/results/`) | — |
| `analysis/defects.py` | 불량 셀 카드 — 어느 셀이 왜 안 잡히는지 (내부용) | — |
| `analysis/rescue.py` | 트레이 내 자리 보정 + 곡선 형상 | — |

셋 다 기존 `predict_xlsx.py` 의 컬럼 인식 규칙을 그대로 쓰므로
**같은 엑셀을 그대로 넣으면 된다.** 원본·셀단위 값은 출력하지 않는다.
